/**
 * 两数之和
 * @group 计算方法
 * @param num1 第一个加数
 * @param num2 第二个加数
 * @returns
 *
 * ### 示例
 * ``` ts
 * countSum(10, 10) // => 20
 * ```
 */
export const countSum = (num1: number, num2: number) => {
  return num1 + num2;
};

